{
  "name": "zettelgarden-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Zettelgarden desktop shell (Tauri v2) wrapping the zettelkasten-front Vite build.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon",
    "tauri": "tauri",
    "test": "vitest run"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.0",
    "vitest": "^3.0.0"
  }
}
